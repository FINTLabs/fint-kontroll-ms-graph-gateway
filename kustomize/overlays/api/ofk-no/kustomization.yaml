apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ofk-no
resources:
  - ../../../base
labels:
    - includeSelectors: true
      pairs:
          app.kubernetes.io/instance: fint-kontroll-ms-graph-gateway_ofk-no
          fintlabs.no/org-id: ofk.no
patches:
  - patch: |-
      - op: replace
        path: "/spec/orgId"
        value: "ofk.no"
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "ofk-no.kontroll.entity.resource-group*"
      - op: replace
        path: "/spec/kafka/acls/1/topic"
        value: "ofk-no.kontroll.event.resource-group*"
      - op: replace
        path: "/spec/kafka/acls/2/topic"
        value: "ofk-no.kontroll.entity.graph-*"
      - op: replace
        path: "/spec/kafka/acls/3/topic"
        value: "ofk-no.kontroll.event.graph-*"
      - op: replace
        path: "/spec/env/0"
        value:
            name: "JAVA_TOOL_OPTIONS"
            value: "-XX:+ExitOnOutOfMemoryError -Xmx9728m"
      - op: add
        path: "/spec/resources"
        value: 
          limits:
            memory: "10Gi"
          requests:
            cpu: "500m"
            memory: "4Gi"

    target:
      kind: Application
      name: fint-kontroll-ms-graph-gateway
  - patch: |-
      - op: replace
        path: "/spec/itemPath"
        value: "vaults/aks-api-vault/items/kontroll-ofk-ms-graph-gateway"
    target:
      kind: OnePasswordItem
      name: fint-kontroll-ms-graph-gateway
